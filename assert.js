/*
The assert module provides a set of assertion functions that are useful for testing and debugging.
It allows you to assert the correctness of values, throw errors if expectations are not met, 
and provide custom error messages.
*/


const assert = require('assert');

assert.strictEqual(8 + 8, 16, 'Expected the sum to be 16');
